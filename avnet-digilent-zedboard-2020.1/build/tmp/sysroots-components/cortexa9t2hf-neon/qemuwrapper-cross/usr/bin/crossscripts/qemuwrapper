#!/bin/sh
set -x

if [ True = False -a "qemuwrapper-cross" != "nativesdk-qemuwrapper-cross" ]; then
	echo "qemuwrapper: qemu usermode is not supported"
	exit 1
fi


qemu-arm -r 3.2.0  -E LD_LIBRARY_PATH=$D/usr/lib:$D/lib "$@"
